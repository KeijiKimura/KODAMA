using UnityEngine;
using System.Collections;

public class CameraScroll : MonoBehaviour {

  /*  public static Vector3 mousePosition;
    public Transform to;
    public Transform from;
    public float percentage = 0f;

	// Use this for initialization
	void Start () {

        mousePosition = Camera.main.ScreenToWorldPoint(Input.mousePosition);
	}
	
	// Update is called once per frame
	void Update () {

        if (Input.mousePosition(Screen.width, Screen.height - 10))
        {
            percentage += Time.deltaTime / 30f;

            transform.position = Vector3.Lerp(from.position, to.position, percentage);
        }
        else if (Input.mousePosition(Screen.width, Screen.height - 100))
        {
            percentage -= Time.deltaTime / 30f;

            transform.position = Vector3.Lerp(from.position, to.position, percentage);
        }
	}*/
}
